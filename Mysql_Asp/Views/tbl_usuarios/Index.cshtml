@model IEnumerable<Mysql_Asp.Models.tbl_usuarios>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.usuarios)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.usuarios)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_usuarios }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_usuarios }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_usuarios })
        </td>
    </tr>
}

</table>
