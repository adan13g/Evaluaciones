@model IEnumerable<Mysql_Asp.Models.tbl_subunidad>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tbl_status.estatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_unidad.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_status.estatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_unidad.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_resultado }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_resultado }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_resultado })
        </td>
    </tr>
}

</table>
